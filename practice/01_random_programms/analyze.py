def analyze_stock(items):
   total = 0
   for item in items:
      maxium = max(item)
      total += item
return total, maximum

items_list = []

while True:
  user_items = int(input('enter item(or exit): '))
     if user_items == 'exit'
   break
else:
