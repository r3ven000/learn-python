#Write a function that takes a number as input and returns its square.
#Requirements:
#Function name: `square(num)`
#Parameter: integer or float
#Return: the number multiplied by itself
#Use the `**` operator (or `*`)
def square(num):
    return num ** 2

print(square(5)) 
print(square(3))
